# docs

Architecture is described in the main [README](../README.md) — the
flow is simple enough (4 nodes, linear) that a diagram is optional.

[`walkthrough.html`](walkthrough.html) is a plain-language, client-facing
demo page — two real runs against the live workflow (a normal inquiry,
and a prompt-injection attempt that got caught by validation), no code
shown. Built 2026-07-12, published as a shareable Claude Artifact (see
`job-search/upwork-profile.md` in the vault root for the live link).
This file is the saved source; open it in a browser to view directly.
