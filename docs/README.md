# docs

Architecture is described in the main [README](../README.md) — the
flow is simple enough (4 nodes, linear) that a diagram is optional.

A canvas screenshot from the n8n editor (`http://localhost:5678`, open
the `inquiry-triage` workflow) is a nice-to-have for a demo but wasn't
required to prove the tool works — that proof is in
[`test-inquiries/test-results.md`](../test-inquiries/test-results.md)
instead. Add one here later if useful for a specific pitch/interview.
